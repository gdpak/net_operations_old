Value source_ip (\d+.\d+.\d+.\d+)
Value protocol (\S+)
Value dest_ip (\d+.\d+.\d+.\d+)
Value dest_port (\d+)
Value source_port (\d+)

Start
  ^.*permitted ${protocol} ${source_ip}\(${source_port}\)(?:\s|-|>)*${dest_ip}\(${dest_port}\) -> Record
